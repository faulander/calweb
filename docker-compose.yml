services:
  calweb:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - /path/to/calibre/library:/library:ro
      - ./data:/data
    environment:
      - SESSION_SECRET=change-me-to-a-random-string
    restart: unless-stopped
